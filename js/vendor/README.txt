/* @supabase/supabase-js v2.116.0 — vendored UMD build.
   Kept in-repo so GabikOS depends on no CDN at run time: it works offline,
   on a restricted network, and behind a strict content policy.
   Replace by copying dist/umd/supabase.js from a newer npm release. */
